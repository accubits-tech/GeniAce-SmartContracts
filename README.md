# GeniAce-SmartContracts

## Preperation
Get [Flow CLI](https://docs.onflow.org/flow-cli/install/) for development and deployment.

## Deployment
Run `flow project deploy --network emulator` to deploy contracts to emulator